# Script for automating the following in a Windows Server environment:
# 
# Assigns the Windows Server a static IPv4 Address
# Assigns the Windows Server a DNS
# Renames the Windows Server
# Installs Active Directory Domain Services
# Creates an Active Directory Forest
# Creates Organizational Units (OU)
# Creates Users
